# Core dependencies for Milano Cortina 2026 Pipeline
pinecone
sentence-transformers
requests
pandas

# Streamlit app dependencies
huggingface-hub
streamlit>=1.30.0

pytest
lxml

# Optional: NLU (entity extraction, better quality scoring)
# Uncomment to enable advanced NLU features:
spacy
# After installing, run: python -m spacy download en_core_web_sm
feedparser

ragas>=0.1.0
datasets>=2.14.0
langchain>=0.1.0
langchain-community>=0.0.10
